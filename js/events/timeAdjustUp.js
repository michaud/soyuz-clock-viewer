export const timeAdjustUp = (
    clientY,
    pointerDownY,
    deviceService,
) => {

    // if(Math.abs(clientY - pointerDownY) < 5) {

    //     deviceService.send('TOGGLE_TIME_ADJUST');
    // }
};
